-- First : Create DATABE if not exists
-- Second : Create the table and add data types
CREATE DATABASE if not EXISTS hbtn_0c_0;

CREATE TABLE IF NOT EXISTS first_table (
	id INT,
	name VARCHAR(256)
)
